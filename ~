#include <stdio.h>
int main() 
{
    char a[]="programming",b[]="language";
    char *p1,*p2;//定义指针变量！
    int i=0;
    p1=a;
    p2=b;//指向a，b的地址
    for(i=0;i<7;i++){
        if(*(p1+i)==*(p2+i)){
            printf("%c ",*(p1+i));
        }
    }
    return 0;
}
